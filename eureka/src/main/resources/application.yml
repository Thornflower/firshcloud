eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/                #多开服务相互注册   防止注册中心挂掉
    register-with-eureka: false                     #服务本身不显示注册中心
  server:
    enable-self-preservation: false             #开发时关闭自我保护
spring:
  application:
    name: eureka
server:                       #端口号
  port: 8761